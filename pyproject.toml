[project]
name = "shared-knowledge-mcp"
version = "0.3.0"
description = "Shared, evidence-backed AI knowledge graph exposed through MCP"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
    "httpx>=0.27",
    "mcp>=1,<2",
    "neo4j-agent-memory[nams]>=0.5,<1",
    "pydantic>=2",
    "pydantic-settings",
    "PyJWT[crypto]>=2.8",
    "starlette",
    "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
]

[project.scripts]
knowledge-mcp = "app.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
