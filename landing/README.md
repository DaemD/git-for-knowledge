# Graphly landing

Static marketing page for Graphly. Not wired into the MCP server yet.

## Preview

Open `index.html` in a browser, or from this folder:

```powershell
python -m http.server 5173
```

Then visit `http://localhost:5173`.
