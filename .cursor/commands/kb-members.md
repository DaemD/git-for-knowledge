# kb members

List owner, members, and pending invites for the current knowledge base.

1. kb_id from `.cursor/kb-id` or `$ARGUMENTS`.
2. Call `kb_members`.
3. Show email, role, status.

Arguments:

$ARGUMENTS
